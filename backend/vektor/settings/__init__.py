"""Пакет настроек проекта «Vektor».

Разделение на ``base`` / ``dev`` / ``test`` соответствует подходу из ADR-0003
(модульный монолит) и issue #1. Окружение задаётся через
``DJANGO_SETTINGS_MODULE`` (см. ``.env.example``).
"""
